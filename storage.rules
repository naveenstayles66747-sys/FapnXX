rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read and write for media uploads (videos, previews, banners, avatars)
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
